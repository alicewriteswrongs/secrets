from distutils.core import setup

setup(
        name = 'secrets',
        version = '0.1',
        packages = ['.'],
        license = 'GPLv3',
        long_description = open('README.md').read(),
)
